/*
 * text block — no decoration; styling lives entirely in the active theme's
 * stylesheet (e.g. wheelercat-cva-blocks.css §TEXT BLOCK). This stub exists
 * so EDS's per-block fetch doesn't 404 on /blocks/text/text.{js,css}.
 */
export default function decorate() { /* no-op */ }
